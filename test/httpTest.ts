import * as db from 'mongo-mock'
import * as DB from '../source/db'
import * as config from '../source/config'
import { expect } from 'chai'
import * as fs from 'fs'

describe('HTTP test', () => {
    it('auth test', () => {

    })
    it('auth with broken credentials test', () => {

    })
})